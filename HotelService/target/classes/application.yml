server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3397/microservices
    username: root 
    password: root 


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  
  application:
    name: HOTELSERVICE

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka

jwt:
  secret: myThirtyTwoCharactersSecretKey1234567890
  expiration-ms: 3600000		
